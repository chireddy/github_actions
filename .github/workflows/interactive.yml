name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Tests
        run: npm test # Modify as needed

  deploy-staging:
    needs: build-test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Staging
        run: echo "Deploying to Staging..." # Replace with actual deploy steps

  approval:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment:
      name: production
      # Add `wait_timer` or `reviewers:` if needed
    steps:
      - name: Wait for Approval
        run: echo "Waiting for manual approval in GitHub UI"

  deploy-production:
    needs: approval
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Production
        run: echo "Deploying to Production..." # Replace with actual deploy steps
